- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles: ''
    ~/.bashrc: bashrc
    ~/.condarc: condarc
    ~/.config/broot: config/broot
    ~/.config/htop: config/htop
    ~/.fzf.zsh: fzf.zsh
    ~/.fzf.bash: fzf.bash
    ~/.gitconfig: gitconfig
    ~/.gitignore_global: gitignore_global
    ~/.shell: shell
    ~/.tmux: tmux
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.zpreztorc: zpreztorc
    ~/.zprezto: zprezto
    ~/.zprezto/runcoms/zpreztorc: zpreztorc

    # add
    # ~/.zprofile: zprofile
    # ~/.bash_profile: bash_profile

    # add?
    # ~/.zlogin: zprezto/runcoms/zlogin
    # ~/.zshenv: zprezto/runcoms/zshenv
    # ~/.pythonrc: pythonrc

    # macos
    # ~/.config/karabiner: config/karabiner
    # ~/.config/btop: config/btop
    # ~/.config/kitty: config/kitty

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    - [chsh -s /usr/bin/zsh, Changing default shell to zsh]
    #  - git update-submodules
